## Communly Frontent

To install all dependencies run `npm install`

Run `npm start` to start in development mode.

For production set env variable `REACT_APP_BACKEND_URL=https://path-to-communly backend` and run `npm start`
